# @package _global_

defaults:
  - override /data: accent
  - override /model: matcha
  - override /callbacks: default
  - override /trainer: default

task_name: "finetune"
run_name: "accent"

seed: 1234

trainer:
  max_epochs: 1000
  gradient_clip_val: 1.0
  devices: 1

model:
  n_spks: 14
  n_vocab: 198
  spk_emb_dim: 64
  
  encoder:
    encoder_params:
      hidden_channels: 256
      filter_channels: 768
      n_channels: 192
      n_feats: 80
      n_layers: 6
      n_heads: 2
      prenet: true
    n_feats: 80
    n_spks: 14

  decoder:
    dropout: 0.05